package com.aluracursos.forumhub.domain.topico.validations.create;

import com.aluracursos.forumhub.domain.topico.dto.CrearTopicoDTO;

public interface ValidarTopicoCreado {

    void validar(CrearTopicoDTO datos);
}
